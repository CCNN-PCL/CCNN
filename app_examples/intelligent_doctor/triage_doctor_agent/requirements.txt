# streamlit==1.32.0  # 已移除：微服务版本不需要streamlit，且与a2a-sdk存在protobuf版本冲突
fastapi==0.110.0
uvicorn==0.27.1
python-multipart==0.0.9
aiohttp==3.9.3
python-dotenv==1.0.1
pydantic>=2.11.3,<3.0.0  # 升级以兼容a2a-sdk，同时满足fastapi要求
email-validator>=2.0.0  # pydantic EmailStr 类型验证所需
sqlalchemy==2.0.28
pillow==10.2.0
requests==2.31.0
DBUtils==3.0.3
nest-asyncio==1.6.0
PyPDF2==3.0.1
PyJWT==2.8.0
cryptography==41.0.7
bcrypt==4.1.2
passlib==1.7.4
itsdangerous>=2.1.0  # OIDC会话管理所需
# 流处理相关依赖
sse-starlette==1.6.5
# 标准A2A SDK支持（用于EntryAgent）
httpx>=0.24.0
a2a-sdk>=0.3.15  # 标准A2A协议SDK (需要Python 3.10+)
# MCP协议支持（用于数据存储服务）
mcp>=0.1.0  # Model Context Protocol客户端
fastmcp>=0.1.0  # FastMCP认证支持
# MySQL支持
aiomysql>=0.2.0  # MySQL异步驱动
pymysql>=1.1.0  # MySQL同步驱动（用于认证适配器）
