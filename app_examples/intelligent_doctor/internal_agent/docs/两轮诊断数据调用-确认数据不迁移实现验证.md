# 两轮诊断数据调用流程分析

## 概述

本文档详细梳理了两轮诊断中数据的调用过程，并确认了数据不可迁移。


## 第一轮诊断数据调用流程

### 1. 数据地址获取阶段

**位置**: `cybertwin_agent_refactored.py` → `_execute_diabetes_demo()`

```687:695:shared/agents/coordinator/cybertwin_agent_refactored.py
data_addresses_round1 = await self.forced_trigger.force_trigger_data_proxy(
    user_input=user_input,
    user_id=user_id,
    intent=intent,  # 使用识别的意图，而不是固定值
    shared_context=shared_context,
    token=id_token  # 传递 id_token
)
```

**说明**:
- CybertwinAgent 通过 `forced_trigger.force_trigger_data_proxy()` 从数据代理应用获取数据地址列表
- 每个数据地址包含 `location` 字段（beijing/shanghai），标识数据所在的地域

### 2. 地域路由阶段

**位置**: `location_router.py` → `route_to_specialists()`

```109:161:shared/agents/coordinator/location_router.py
def route_to_specialists(
    self,
    intent: str,
    data_addresses: List[Dict[str, Any]],
    specialist_instances: Dict[str, Dict[str, Any]]
) -> List[Tuple[str, str, List[Dict[str, Any]]]]:
    """
    路由到专科医生实例
    
    参数：
        intent (str): 意图类型
        data_addresses (List[Dict[str, Any]]): 数据地址列表
        specialist_instances (Dict[str, Dict[str, Any]]): 专科医生实例字典
            格式: {
                "internal_medicine": {
                    "beijing": InternalMedicineAgent实例,
                    "shanghai": InternalMedicineAgent实例
                },
                "surgical": {...}
                # "image_analysis": {...}  # 暂时注释掉影像分析
            }
        
    返回：
        List[Tuple[str, str, List[Dict[str, Any]]]]: 路由结果列表
            每个元素为 (location, specialty, data_addresses)
            例如: [("beijing", "internal_medicine", [地址1, 地址2])]
    """
    # 1. 选择专科类型
    specialty = self.select_specialty_by_intent(intent)
    
    # 2. 按地域分组数据地址
    grouped_addresses = self.group_data_addresses_by_location(data_addresses)
    
    # 3. 构建路由结果
    routing_results = []
    
    # 获取该专科类型的所有实例
    specialty_instances = specialist_instances.get(specialty, {})
    
    for location, addresses in grouped_addresses.items():
        # 检查是否有对应地域的实例
        if location not in specialty_instances:
            self.logger.warning(
                f"[LocationRouter] 地域 {location} 没有 {specialty} 专科医生实例，跳过"
            )
            continue
        
        routing_results.append((location, specialty, addresses))
        self.logger.info(
            f"[LocationRouter] 路由到: {location} 的 {specialty} 专科医生, "
            f"数据地址数量: {len(addresses)}"
        )
    
    # 如果没有匹配的数据地址，但需要诊断，可以路由到默认地域
    if not routing_results and specialty_instances:
        # 选择第一个可用的地域实例
        default_location = list(specialty_instances.keys())[0]
        routing_results.append((default_location, specialty, []))
        self.logger.info(
            f"[LocationRouter] 没有匹配的数据地址，路由到默认地域: {default_location}"
        )
    
    return routing_results
```

**关键步骤**:
1. **按地域分组数据地址**: `group_data_addresses_by_location()` 根据数据地址的 `location` 字段进行分组
2. **路由到对应地域的专科智能体**: 每个地域的数据地址只路由到对应地域的专科智能体实例



### 3. 专科智能体数据获取阶段

**位置**: `two_round_diagnosis_coordinator.py` → `first_round_diagnosis()`

```132:143:shared/agents/coordinator/two_round_diagnosis_coordinator.py
specialist_tasks = []
for location, addresses in location_groups.items():
    specialist = self._get_specialist(specialty_type, location)
    if specialist:
        self.logger.info(f"[TwoRoundDiagnosisCoordinator] 路由到 {specialty_type} - {location}")
        print(f"  路由到专科医生: {specialty_type} - {location}")
        task = specialist.execute(
            user_input=user_input,
            user_id=user_id,
            data_addresses=addresses,
            shared_context=shared_context
        )
        specialist_tasks.append(task)
```

**说明**:
- 每个地域的专科智能体只接收该地域的数据地址列表
- 专科智能体的 `execute()` 方法接收 `data_addresses` 参数

### 4. 专科智能体从数据存储服务读取数据

**位置**: `internal_medicine_agent.py` → `_read_data_from_addresses()`

```488:526:shared/agents/specialists/internal_medicine_agent.py
# 过滤出当前地域的数据地址
location_addresses = [
    addr for addr in data_addresses
    if addr.get("location") == self.location
]

if not location_addresses:
    self.logger.info(f"[{self.name}] 没有匹配当前地域({self.location})的数据地址")
    return {
        "medical_history": None,
        "medications": None,
        "lab_results": None,
        "sources": []
    }

self.logger.info(f"[{self.name}] 找到 {len(location_addresses)} 个数据地址")


**MCP协议支持**:
- `DatabaseStorageClient` 支持MCP协议和HTTP协议，可通过配置切换
- 如果配置了 `USE_MCP_PROTOCOL=true`，会使用 `MCPStorageClient` 通过MCP协议从数据存储服务获取数据
- 如果未启用MCP协议，会使用HTTP协议作为降级方案

**数据不可迁移性保证**:
- 专科智能体有 `self.location` 属性，标识其所属地域
- 只处理 `location == self.location` 的数据地址
- 通过 `DatabaseStorageClient` 调用对应地域的数据存储服务URL（beijing_url 或 shanghai_url）

### 5. 数据存储服务按地域读取数据

**位置**: `database_storage_client.py` → `retrieve_medical_data()`

```183:186:shared/agents/utils/database_storage_client.py
# 如果使用MCP协议，调用MCP客户端
if self.mcp_client:
    self.logger.info("[DatabaseStorageClient] 使用MCP协议读取数据")
    return await self.mcp_client.retrieve_medical_data(data_addresses, user_id)
```

**MCP协议流程**（如果启用）:
1. **检查MCP协议是否启用**: 如果 `USE_MCP_PROTOCOL=true` 且 `MCPStorageClient` 可用，使用MCP协议
2. **调用MCP客户端**: `MCPStorageClient.retrieve_medical_data()` 通过MCP协议从数据存储服务获取数据
3. **按地域和MCP服务器分组**: MCP客户端按地域和MCP服务器URL组合分组数据地址
4. **并行处理各地域数据**: 并行调用各地域的MCP服务器获取数据


**关键步骤**:
1. **协议选择**: `DatabaseStorageClient` 优先使用MCP协议，如果未启用则使用HTTP协议
2. **根据地域获取服务URL**: 根据 `location` 获取对应地域的数据存储服务URL（MCP服务器URL或HTTP URL）
3. **调用对应地域的服务**: 直接调用对应地域的服务端点

**数据不可迁移**:
-  数据存储服务按地域部署，每个地域有独立的服务URL（MCP服务器URL或HTTP URL）
- 北京数据只调用北京的数据存储服务（`beijing_url` 或 `MCP_SERVER_BEIJING_URL`）
- 上海数据只调用上海的数据存储服务（`shanghai_url` 或 `MCP_SERVER_SHANGHAI_URL`）
- 数据在对应地域的服务中处理，不会跨地域传输

---

## 第二轮诊断数据调用流程

### 1. 数据代理应用直接返回健康监测数据

**位置**: `data_proxy_client.py` → `interact_with_context()`


**说明**:
- 第二轮诊断时，数据代理应用直接返回健康监测数据（`medical_data`）
- 数据通过 `shared_context.direct_medical_data` 传递给专科智能体
- **无需通过数据存储服务**，数据代理应用直接返回数据

### 2. 专科智能体接收直接数据

**位置**: `internal_medicine_agent.py` → `execute()`
"
```

**说明**:
- 专科智能体检查 `shared_context.direct_medical_data`
- 如果存在直接数据，优先使用直接数据，跳过数据存储服务请求

### 3. 专科智能体处理直接数据

**位置**: `internal_medicine_agent.py` → `_read_data_from_addresses()`
```

**数据不可迁移性保证**:
- 健康监测数据由数据代理应用直接返回，不涉及跨地域数据迁移
- 数据在专科智能体本地处理，不会传输到其他地域

---


-
## 验证点

### 第一轮诊断验证

1. **数据地址包含location字段**: 每个数据地址都有 `location` 字段（beijing/shanghai）
2. **LocationRouter按地域分组**: `group_data_addresses_by_location()` 按地域分组数据地址
3. **路由到对应地域的专科智能体**: 每个地域的数据地址只路由到对应地域的专科智能体
4. **专科智能体过滤当前地域的数据地址**: `location_addresses = [addr for addr in data_addresses if addr.get("location") == self.location]`
5. **调用对应地域的数据存储服务**: `DatabaseStorageClient` 根据 `location` 调用对应地域的URL

### 第二轮诊断验证

1.  **数据代理应用直接返回数据**: `shared_context.direct_medical_data` 包含健康监测数据
2.  **专科智能体直接使用数据**: 跳过数据存储服务请求，直接使用 `direct_medical_data`
3.  **数据在本地处理**: 数据在专科智能体本地处理，不涉及跨地域数据迁移

---



