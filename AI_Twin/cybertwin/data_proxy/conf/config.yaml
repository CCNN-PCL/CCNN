server:
  address: ":8080"

db:
  host: "localhost"
  port: 3306
  username: "root"
  password: "password"
  database: "cybertwin"
  max_idle_conns: 10
  max_open_conns: 100
  conn_max_lifetime: "1h"

log:
  file_path: "./logs/app.log"
  verbose: true
  debug: false
