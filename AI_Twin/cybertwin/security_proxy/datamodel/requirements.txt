# 核心Web框架
fastapi==0.104.1
uvicorn==0.24.0.post1

# 机器学习依赖
scikit-learn==1.7.2  # 与训练时版本一致，避免模型加载问题
joblib==1.3.2        # 用于加载TF-IDF向量器和决策树模型
numpy==1.26.2        # 处理数值计算（模型预测可能用到）

# 其他工具（可选，如日志、编码处理等）
python-multipart==0.0.6  # FastAPI处理表单数据（当前代码未用，预留）